Minesweeper!

Image resources at mulan.csufresno.edu/~twilson/csci130/imgs/

grid generated by JS table like in last lab.

Handlers:
    Make sure to ignore certain clicks.
  onClick (left click)
  onContextMenu (right click)


updateTimer()
  may be tricky to make accurate.

Board array.
visib array to indicate covered, empty or flag

reveal(n) function.


Timeout must be turned off when game is over.
In the reveal move as it's revealing if a flag covered spot is revealed to have no bomb,
make sure to adjust flags value accordingly and what not.
